# Agent Instructions: moroshani/moroshani

This is the public GitHub profile README repository for `moroshani`.

## Scope

Primary file:

```text
README.md
```

The README is displayed on `https://github.com/moroshani`, so keep it concise, public-facing, and useful to a first-time visitor.

## Public Positioning

Keep these project roles clear unless the user explicitly changes strategy:

- `Dominoyar`: public product home only. Do not add product source code or private-source implementation details here.
- `setadinfo`: public/open-source application project.
- `solar-hijri-calendar-component`: public/open-source package project.

## Safety

- Do not add tokens, private local paths, private repo URLs, private email addresses, phone numbers, or unpublished business details.
- Do not invent contact links, websites, app-store links, screenshots, claims, employers, locations, or credentials.
- Keep visitor-facing copy direct and modest. Prefer concrete project descriptions over hype.

## Change Checklist

After editing:

1. Run `python3 scripts/check_profile.py` if available.
2. Confirm `README.md` still links to the active public repos.
3. Commit and push a small focused change.
